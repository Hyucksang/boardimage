    <div class="title">
       title :  <%= @post.title %>
    </div>
    <div class="content">
    <%= @post.content %>
    </div>

<%= link_to 'Edit', edit_post_path %>
<%= link_to 'Delete', post_path(@post), method: :delete,data: {confirm: 'U sure'} %>
